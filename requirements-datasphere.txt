# Зависимости УДАЛЁННОЙ задачи обучения, исполняемой на Yandex DataSphere.
#
# ВАЖНО: на DataSphere используется Python 3.10 (локально у нас 3.11+).
# Поэтому окружение удалённой задачи описывается отдельно и собирается под 3.10,
# а не копируется из локального requirements.txt (там, например, web/db-пакеты,
# которые на воркере не нужны, и версии, привязанные к локальному интерпретатору).
#
# Здесь — только то, что нужно коду app/ml/* (trainer, preprocessing, validator,
# callbacks, data_loader). Web/DB-слой на DataSphere не запускается.
#
# Будет использовано в config.yaml DataSphere-задачи на этапе реализации
# DataSphereRunner (см. app/jobs/datasphere_runner.py). Пока не задействовано.

torch==2.5.1            # версия с колёсами под cp310 (CUDA-канал указывается в job-конфиге)
pytorch-lightning==2.6.5
torchmetrics==1.9.0
darts==0.44.1
scikit-learn==1.7.2
numpy==1.26.4
pandas==2.3.3
scipy==1.15.3
aiomoex==2.1.2
aiohttp==3.14.1
fastparquet==2026.5.0
